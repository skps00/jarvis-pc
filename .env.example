# Copy to .env and fill. Never commit .env.

# Google Cloud STT (yue-Hant-HK) — choose one auth style your client uses
# GOOGLE_APPLICATION_CREDENTIALS=C:\path\to\service-account.json

# Intent LLM (small model JSON) — pick provider later
# OPENAI_API_KEY=
# ANTHROPIC_API_KEY=
# GOOGLE_API_KEY=
