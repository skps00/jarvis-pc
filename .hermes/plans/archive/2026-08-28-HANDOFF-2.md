# 2026-08-28 進度 Handoff #2（凌晨 session，下個 session 用）

> 新 session 第一件事：讀呢個檔 + `TODOS.md` + `JARVIS_MASTER_PLAN.md`；SK 專案優先序 = **mod → jarvis → earth**。
> ⚠️ **SK 新指示（2026-08-28）**：**所有 coding 交 Cursor**（`agent -p --trust`），Hermes 負責診斷/測試/驗證/部署——唔好再直接 patch code。

## 今次 session 完成

### 1. AGENTS.md 更新 ✅（SK 已 approve）
- Home 根 `C:\Users\skps9\AGENTS.md` 加「## 工程判斷（Engineering judgment）」section（senior engineer mindset：操作者可能錯、唔盲從 suggestion、inspect+驗證假設、唔 fake success、對最終結果負責）
- Workflow 第 2 步加「驗證假設（含『SK 講嘅都可能錯』）」
- 流程：寫 draft（AGENTS.md.next）→ VS Code `code --diff` 俾 SK review → approve 後覆蓋

### 2. Jarvis device 去重 + 存名修復 ✅（code 完成，23 tests 全綠）
- **根因**：settings UI 用緊未去重嘅 `list_input_mics()`；`list_input_mics_deduped()` 寫咗但冇人用；output 冇 dedup 版；save 存 index（重開機 reorder 指錯）
- 改咗（**已經係 Cursor 指示前**，下次要經 Cursor）：
  - `settings_ui.py`：mic/speaker 下拉改用 deduped 版（**input 26→20、output 53→39**）；新增 `list_output_speakers_deduped()`；`_pick_device_label` 對 int reorder 用 device 名反查；save 改存**名**（`_mic_label_to_name`/`_spk_label_to_name`）
  - `settings.py`：`tts_output_device` 支援存名（str 保留，空→None；原本 str 會變 None）
  - `mouth.py`：新增 `_resolve_output_device()`（str 名→44.1k index）——**原本存名會 crash**（`int(device)` ValueError）
  - tests：`test_settings.py`、`test_wake.py` 更新（pre-existing fail 修正：CUSTOM_DEFAULT_THRESHOLD 已改 0.50 但 test 未跟）
- 驗證：23 個相關 test 全綠；全套 164 passed + 3 **pre-existing** fail（`test_brain.py` x2、`test_router.py`——同今次改動無關）

### 3. settings.json 存名 ✅（備份咗兩次）
- `wake_mic_device: 20` → **「麥克風 (2- Arctis Nova 7)」**（resolve → MME index 2，16k 必開到）
- `tts_output_device: 55` → **「耳機 (Arctis Nova 7)」**（resolve → 55）
- 備份：`settings.json.bak-20260828-034225`

### 4. Serve 重啟 ⚠️（學到 restart child 坑）
- **坑**：serve 會 spawn pythonw restart child（hold 住 serve lock）——kill parent 後 child 接手但 MCP 唔起（8765 冇 listening）。JARVIS.vbs 新實例會因 lock 被佔而「已在運行」退出
- **正解**：重啟 serve = 殺晒**所有** jarvis python process（parent + child + poller）→ 確認 8765 釋放 → `cscript //nologo JARVIS.vbs`
- 最後狀態：**PID 47184 python.exe serve + MCP 8765 listening ✅ + wake_loop_start 03:53:43**

### 5. Speaker gate 問題 → 暫時關閉 ⏸（SK 決定擱置）
- **「叫唔醒」真正元兇**：`spk_gate reject score=0.178`（threshold 0.5）——wake 聽到但 gate 唔放行
- **離線實測**（12 個 7/30 真聲樣本 vs 8/27 profile）：cosine **0.18-0.38 全部 reject**——同一把聲但錄音鏈路唔同（WDM/MME/Sonar 虛擬 device）→ ECAPA cross-link 敏感
- SK：「same mic, mic too shit, ignore this first」→ **settings.json `speaker_gate: false`**（備份 `settings.json.bak-20260828-035335`）→ wake 即刻 work（wake_debug `spk_gate off`）
- 日後開返：`scripts/enroll_voice.py`（用而家 default mic MME 錄 15s）→ threshold 可試 0.35

## 下一步（按優先）

1. **SK 試「hey jarvis」**——確認 wake 而家 work（gate 已關）
2. **Cursor 第一個 job**：`powershell -NoProfile -ExecutionPolicy Bypass -File C:\Users\skps9\AppData\Local\cursor-agent\cursor-agent.ps1 -p --trust "prompt"`（例：mod `datapack_lookup` D17-A / review jarvis 今次 diff）
3. **Speaker gate 收尾**（低優先）：SK 用 Arctis re-enroll → 開返 gate
4. **mod backlog**：`lock-only JEI craft-empty`（D14-A）
5. **earth**：封測 F.2/F.4 實機（S24U，SK 手測）

## 關鍵狀態

- **JARVIS serve**：PID 47184（`python.exe -m jarvis serve`）行緊；MCP 8765 LISTENING；**speaker_gate OFF**；wake mic = MME index 2（存名 resolve）；log → `%APPDATA%\Jarvis\serve.log`
- **Hermes config**：session_reset both/180/6（idle 3h / daily 6:00 自動 reset）；SK 知道 `/new` 最快慳 context
- **AGENTS.md**：已更新（工程判斷 section）
- **背景 process**：serve 47184 + poller 46264（唔好亂 kill——要 kill 就全部一齊）

## 已知坑（新增）

- Serve restart child：kill parent 唔夠，要殺晒 jarvis python 先重啟
- `tts_output_device`/`wake_mic_device` 而家**存名**（str）——手改 settings.json 要寫名，唔好寫 index
- Speaker gate ECAPA：同 mic 唔同 host API 鏈路都會跌 score（0.18-0.38）——enrollment 同 runtime 必須同鏈路
- coding 交 Cursor 後，Hermes 唔好直接 patch src/（除非 SK 明示）
