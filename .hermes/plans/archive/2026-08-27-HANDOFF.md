# 2026-08-27 進度 Handoff（開新 session 繼續用）

> 新 session 第一件事：讀呢個檔 + 讀 master plan + session_search「speaker gate」撈返今日完整對話。

## 今日完成

### Tier 1 全部 coding 完成（真機測試部分通過）
- `src/jarvis/wake.py`：rearm timeout（`_REARM_TIMEOUT_S=3.0`）、D3（`_CMD_SILENCE_FRAMES=5`、`_STT_TRAIL_S=0.35`）、`_CMD_SPEECH_RMS=0.02`、VAD disabled（`vad_threshold=0`）
- `src/jarvis/ear.py`：GPU `device="cuda:0"`（SenseVoice rtf 0.235 實測 ✅）
- `src/jarvis/mouth.py`：`interrupt()`（sd.stop 中斷當前 TTS）
- `src/jarvis/shell_app.py`：STT warmup（預載 SenseVoice）、「Yes Sir」搬去 capture 完成即刻唸、reply 前 `interrupt()`
- `src/jarvis/settings.py`：`tts_ack: bool = True`
- `src/jarvis/asr_repair.py`：ice/eyes→CS 修正

### Tier 2 聲紋 enrollment 完成 ✅
- 聲紋 profile：`%APPDATA%/Jarvis/voice_profile.npy`（192d ECAPA embedding，已驗證非零有效）
- model cache：`jarvis-pc/.cache/ecapa-voxceleb/`（embedding_model.ckpt + hyperparams.yaml 有；**label_encoder.ckpt 缺失但 encode 唔使，唔影響**）

### Bug fix
- `wake_mic_device` 3→2（Sonar Stream 虛擬 device RMS 0 死 → 實體 Arctis Nova 7）
- `wake_threshold` 0.41→0.35（用戶：唔太敏感）

## 下一步（按優先順序）

1. **Speaker gate 真聲驗證（最優先，2026-08-27 晚 code 已完成上線）**：
   - Code：`src/jarvis/speaker_gate.py`（ECAPA verify）+ `wake.py` gate（OWW/STT 兩 path）+ settings（`speaker_gate=true`, `speaker_threshold=0.5`）+ settings UI + `scripts/verify_voice.py`（3s 錄音睇 score）
   - serve 已重啟：`spk_gate ready dev=cuda:0 thr=0.5 load=1.0s`
   - **⚠️ 關鍵發現**：210 個 `my_real_samples` 係 **Sonar mic**（record_jarvis_wake.py 用 default input=Sonar），enrollment 用 **Arctis**（wake_mic_device=2）→ cross-mic 對 profile 只有 0.19-0.33，**樣本驗證唔到 accept path**。profile 好可能冇問題（真 30s Arctis 錄音），但要 SK 真聲 confirm。
   - **驗證方法**：SK 講「hey jarvis」→ 睇 wake_debug.log `spk_gate accept/reject score=`；或 `python scripts/verify_voice.py`（錄 3s print score）。reject 或 score<0.5 → 重新 `python scripts/enroll_voice.py`（30s，有 RMS check + meta）。threshold 有真數據再 tune。
2. **AEC**：spike `python-webrtc-audio-processing`（WebRTC AEC3，Zoom/Meet 用嗰個）喺部機裝唔裝到；speexdsp fallback。
3. **Streaming TTS**：Hermes 流式輸出 + JARVIS 逐句唸（首句 1-2s 出聲）。
4. **狀態檢測 daemon**：cron 最細分鐘（`15s` 報 Invalid duration），要自己寫秒級 daemon（滑鼠移動 + 遊戲進程實時判斷）。

## 關鍵狀態
- settings.json（`%APPDATA%/Jarvis/settings.json`）：`wake_mic_device=2`、`wake_threshold=0.35`；backup `.bak-wakemic`
- jarvis serve 跑緊（thr=0.35，冇 error）
- 聲紋實測：同人 0.9142 vs 異人 -0.02~0.20；threshold 0.5
- Piper TTS 合成 0.266s（非 bottleneck，LLM 首 token 先係）

## 已知坑
- ECAPA symlink 要 monkeypatch `link_with_strategy`→copy（Windows 無 symlink 權限）
- label_encoder.ckpt 缺失但 encode_batch 唔使（唔影響 embedding）
- 「開cs」SenseVoice 轉錄做「ice」→ asr_repair 已修但未真機驗證
- Windows mic device index 重開機會變 → 揀 device 用名「麥克風 (Arctis Nova 7)」唔用 index
- funasr 1.3.29 `models/encoder` 缺失只影響舊 serve；新正式 serve 乾淨
