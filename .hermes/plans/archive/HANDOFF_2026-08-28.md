# JARVIS ONE — Handoff（2026-08-28）

> 今日 session 完成嘅嘢 + 系統現狀 + 下次起點。俾下一個 session / agent 快速接上。

---

## 系統現狀（而家跑緊）

- **JARVIS ONE exe**：`C:\Users\skps9\Documents\Code_Project\jarvis-hud\dist\JARVIS-ONE-0.3.1.exe`（72MB portable，含 refactor）——**0.3.0 而家運行緊**（SK 喺 restart 後開嘅）；桌面/開始/Startup 捷徑**已指 0.3.1**（下次 login 自動開 0.3.1，或手動 tray → 退出 → 開 0.3.1）；0.2.0/0.3.0 保留 rollback
- 開機自動：Startup 捷徑 → exe
- Watchdog cron：`jarvis-serve-watchdog`（每 1m，no_agent）——serve 死咗自動重啟
- Self-monitor cron：`jarvis-self-monitor`（每日 09:00，no_agent）

## 今晚完成（2026-08-28 晚，全部 CDP 實測驗證）

### GUI 全按鈕實測（SK 出手截圖 + 我 CDP 驗證）
Settings：scan 19/38 裝置 ✓、開 Hermes 網頁 ✓、檢測 API ✓（8688 冇服務）、試語音提醒 ⚠️、儲存/取消/✕/checkbox/dropdown/slider 全 ✓
Dock：media 3 掣 + 10 apps render ✓（dev 版）
Companion：Clear/Close ✓
HUD：display-only

### 修咗 4 個 bug（cursor-agent 實作，已驗證）+ 2 個運行時問題
1. **試語音提醒冇聲**：`main.js settings:test-alert` 改 `blocking=True`（原本 blocking=False + execFile 短命 process → daemon thread race，TTS 未播完 process 已死，UI 顯示成功但冇聲）
2. **exe Dock apps 唔見**：`package.json build.files` 漏咗 `apps.json` → asar 冇 apps.json → APP_DEFS 空 → dock 空白。已加 + 重新打包 0.3.0/0.3.1（asar 已驗證有 apps.json）
3. **裝置揀唔到 + 名截斷**：settings.html datalist → 自訂 `wakeMicPicker`/`ttsOutputPicker` dropdown（scan 後展開、click option 填完整名、click 外關閉）；scan-hint 10px→12px 對比提升
4. **Hermes 8688 硬編碼**：Settings 加 `hermes_base_url` field（settings.json 可配置，default http://127.0.0.1:8688）；open-hermes/probe-hermes 讀 settings
5. **serve console window「JARVIS - 就緒」**：watchdog（`%LOCALAPPDATA%\hermes\scripts\jarvis_watchdog.py`）spawn serve 時加 `JARVIS_ELECTRON_HOST=1`（原本冇 → Tkinter UI 彈出）——已修
6. **restart 後 HUD 唔見**：portable exe relaunch race（0.3.0 乾淨重啟後正常）；另有遊戲中自動隱藏設計（checkActivity playing → win.hide()）——SK 打機時撳「顯示 HUD」會被 5 秒後再 hide，屬預期行為

### 新功能：Main GUI / Home Page（0.3.0）
- `home.html` + `home-preload.js`：J.A.R.V.I.S. Home window（720x480 frameless Iron Man 主題）
  - 對話 feed：USER ▸ / JARVIS ▸ / SYSTEM ▸ entry + 時間戳（防重複 last line）
  - 文字輸入框：`home:send-text` IPC → execFile `execute_utterance(text, ask_confirm=lambda p: True, repair_asr=False)`（120s timeout）→ parse `[caption]`/`[speak]`/`[fail]`/`[route]` lines
  - 狀態燈（voice_status.json 2s poll）、Weather/HW/Wake live cards、快捷掣（Companion/Settings/Toggle HUD/Show Dock）、Quit/Relaunch
  - replyServer（8770 /reply）→ `homeAppend` 推送去 Home
- Tray menu 加「主畫面 / Home」；tray 雙擊開 Home；second-instance 開 Home
- **啟動行為：預設唔自動開**（`JARVIS_OPEN_HOME=1` 先開）——SK 原則啟動靜默 tray
- 已實測：send「現在幾點」→ USER/SYSTEM/JARVIS 完整回覆鏈路 ✓

### 測試 SOP
`docs/TESTING_SOP.md` 有舊版；新流程：`JARVIS_OPEN_HOME=1 JARVIS_OPEN_SETTINGS=1 ./node_modules/.bin/electron . --user-data-dir=C:\Temp\jarvis-dev-settings --remote-debugging-port=9222` → CDP (`C:\Temp\cdp\cdp_eval.js <wsUrl> <expr>`) 讀 DOM 驗證

## 剩低 / 下次起點

1. **換 0.3.1**：tray → 退出 JARVIS → 開 `JARVIS-ONE-0.3.1.exe`（或下次 login Startup 自動）；而家行緊 0.3.0（同 0.3.1 功能一樣，0.3.1 多 refactor+error log）
2. **GUI 實測 0.3.1**（SK 出手）：Home 對話、Settings picker、tray menu、Dock icons（而家應該有 10 apps）
3. **Voice input**（SK 買新 mic 後）：mic gain 正常 → AGC 唔使極端 → 重新校 threshold + speaker gate enrollment
4. **HWiNFO SHM**：SK 開 HWiNFO Shared Memory Support 先有數據
5. **Hermes 升級**：`hermes update --check` 話有更新（v0.20.0 behind origin/main）——升級會 restart gateway + kill 緊跑緊嘅 agent（包括 Discord session），**要 SK 確認時機**（唔好同 JARVIS serve 重啟同時做）；做之前 `git -C "$LOCALAPPDATA/hermes/hermes-agent" log --oneline -20 origin/main` 睇改咗咩
6. **Code review 跟進**：`%TEMP%\jarvis_code_review_2026-08-28.md` 有 pass1/pass2 報告——下次迭代優先序：輸入消毒（focusApp/launchApp/tasklist）、home:send-text ask_confirm 拆出、openExternal 白名單、will-quit teardown、8770/8771 auth+body 上限、settings:save key allowlist、路徑配置化、**main.js 切模組（836 行單檔）**
7. **Mage-VL streaming gate**：mamba_ssm Windows 唔 practical——deferred

## 關鍵路徑 / 指令

- HUD repo：`C:\Users\skps9\Documents\Code_Project\jarvis-hud`（exe 喺 dist/）
- JARVIS repo：`C:\Users\skps9\Documents\Code_Project\jarvis-pc`（src/jarvis/）
- 啟動：`JARVIS-ONE-0.3.0.exe`（或 npm start dev 版）
- 重啟：tray → 重新啟動
- Log：`%APPDATA%\Jarvis\serve.log`、`wake_debug.log`、`voice_status.json`、`self_monitor.log`
- Health：`http://127.0.0.1:8765/health`
- 計畫：`jarvis-pc\.hermes\plans\JARVIS_MASTER_PLAN.md`、`REMAINING_WORK.md`、`PHASE8_JARVIS_ONE.md`
- Code review 報告：`%TEMP%\jarvis_code_review.md`、`jarvis_future_review.md`
- Cursor CLI：`C:\Users\skps9\AppData\Local\cursor-agent\versions\2026.08.25-3e8eec8\cursor-agent.cmd agent --trust -p '...'`

## 已知問題 / 陷阱

- **cursor-agent 唔好信佢會做晒 core**：佢傾向自己做設計/加嘢、漏 prompt 指定嘅核心功能（對話 feed 漏咗兩次）。**prompt 要極具體**（function 名、行號、行為），做完整要 grep 驗證（sendText/onAppend 等 bridge 最容易漏）
- **mic 太細聲**（講嘢 rms 0.003-0.04）：Windows level 已 100%——Sonar/Arctis 硬件層面——等新 mic
- **誤觸 vs 叫醒**：threshold 0.45 係平衡點（settings.json 已確認）；新 mic 之後重新校
- 打包後 npm start 版同 exe 版唔好同時開（single instance lock 會擋——正常；dev 測試用 `--user-data-dir` 分開）
- Python 用 `C:\Users\skps9\AppData\Local\Python\pythoncore-3.14-64\python.exe`（Py3.14；pip 要 `env -u PYTHONPATH`）
- 打包驗證 asar：`npx asar list "dist/win-unpacked/resources/app.asar" | grep apps.json`
