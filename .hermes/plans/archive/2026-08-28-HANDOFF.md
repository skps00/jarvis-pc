# 2026-08-28 進度 Handoff（下個 session 用）

> 新 session 第一件事：讀呢個檔 + `TODOS.md`（已合併主線）+ `JARVIS_MASTER_PLAN.md`；SK 專案優先序 = **mod → jarvis → earth**（CS_asstant ignore）。

## 今日完成

### 1. 🔴 Popup 彈窗謎底 —— 破解＋根治 ✅
- **兇手**：`Hermes_Gateway_Watchdog` 排程任務（每 2 分鐘 `powershell -WindowStyle Hidden -File ...hermes-gateway-watchdog.ps1`）—— Task Scheduler 先建 console 窗後隱藏，~500ms 閃。
- **修復**：task action 改 `wscript.exe` + `hermes-gateway-watchdog-launch.vbs`（`sh.Run "...", 0, False`）—— 23:01 tick 實測零閃。
- **探測工具**（保留）：`%LOCALAPPDATA%\hermes\scripts\flash_catch.py`（pythonw + ctypes 8ms poll GetForegroundWindow；log `%TEMP%\flash_catch.log`）。
- 教訓：`-WindowStyle Hidden` 喺 Task Scheduler 照彈；wscript GUI-subsystem 先係根治。已入 skill `windows-focus-safe-automation`。

### 2. Volcengine Ark —— 裝好，擱置 ⏸
- Provider plugin 已寫（`hermes-agent/plugins/model-providers/volcengine/`）、aliases `glm5`/`dsvolc` 已設、`VOLCENGINE_API_KEY` 入 `.env`。
- ⚠️ 401 = **未實名認證**；HK 個人認證掃臉**只支援大陸身份證**（銀行卡要內地卡）→ **SK 擱置**。plugin 保留日後用。
- 如果日後要 GLM：直接 Zhipu 官方（z.ai 接受香港用戶）更簡單。

### 3. Cursor CLI —— 正式上線 ✅
- 獨立 agent CLI 已裝：`%LOCALAPPDATA%\cursor-agent\`（agent.cmd / cursor-agent.ps1），login 咗 **skps952608@gmail.com**。
- **用法**（headless，用 SK plan quota）：
  ```
  powershell -NoProfile -ExecutionPolicy Bypass -File C:\Users\skps9\AppData\Local\cursor-agent\cursor-agent.ps1 -p --trust "prompt"
  ```
- 實測：`agent -p --trust "Reply with exactly: CURSOR_OK"` → CURSOR_OK ✅
- 坑（已入 skill `cursor-cli-integration`）：**內嵌 cli.js 冇 `agent` subcommand**（editor CLI table，點試都靜默 exit 0）；`NO_OPEN_BROWSER=1 agent login` 印 URL 唔彈窗；`-p` flag 必帶；首次要 `--trust`。

### 4. 三批未 commit（SK：唔使 commit）
| Repo | 內容 | 驗證 |
|---|---|---|
| super_minecraft_AI_player | tooltip chrome scrub（21 files, +411/−28） | checks ✅ + gradle compile ✅ |
| jarvis-pc | speaker gate＋wake＋hands/bridge＋alerts（23 files, +1,112/−388） | pytest 40 ✅ |
| Earth_Online_App v2.0 | life ledger＋chips＋beta UX＋i18n（33 files, +1,035/−117） | tsc ✅ |

### 5. Job Inventory 盤點（全機）
- TODO 來源：mod TODOS.md（8 項 backlog，最前 = `lock-only JEI craft-empty` D14-A）、jarvis TODOS.md（已合併主線，見下）、Earth TODO.md（封測 F.2/F.4 要實機）、CS_asstant todo_later.md（ignore）、Earth v1.0 TODO（legacy ✅）。
- Cursor 數據發現 `github.com/skps00/battlenow-a-rpg-game` repo——**本地冇**，未確認係咪舊 project。

## 下一步（按優先）

1. **Speaker gate 收尾**（jarvis 主線最前）：SK 用 **Arctis**（唔係 Sonar）錄 ~30s 重新 enrollment（`scripts/enroll_voice.py`）→ `scripts/verify_voice.py` 驗證（同 mic 應 0.6+）→ threshold 0.5 微調。而家 cross-mic 得 0.13-0.38。
2. **Cursor 第一個 job**：用 `agent -p --trust` 跑（例：mod `datapack_lookup` D17-A、或者 review 三批 diff）。
3. **mod backlog**：`lock-only JEI craft-empty`（D14-A）起步。
4. **earth**：封測 F.2/F.4 實機（S24U，SK 手測）。

## 關鍵狀態

- **JARVIS serve**：PID 48712（`pythonw -m jarvis serve`）行緊；`spk_gate ready dev=cuda:0 thr=0.5`；MCP 8765 LISTENING；log → `%APPDATA%\Jarvis\serve.log`。
- **Wake**：heartbeat（60s）+ peak tracking 上線；`wake_mic_device` 存名（44.1k entry）。
- **環境**：system python 3.14 + speechbrain + CUDA OK；hermes venv = uv-managed（python.exe 係 launcher，彈窗陷阱 → 背景 spawn 用 pythonw）。
- **config**：session_reset both/180/6 已生效；approvals smart；deny list 已修復。
- **背景 process**：無（flash_catch 120min 窗已自然結束；proc_watch 已 kill；cmd_watch 已 kill）。

## 已知坑（新增）

- Task Scheduler + powershell `-WindowStyle Hidden` 照彈 → 用 wscript+VBS。
- 內嵌 Cursor cli.js 冇 agent subcommand → 用獨立 `%LOCALAPPDATA%\cursor-agent\cursor-agent.ps1`。
- 火山方舟 401 = 未實名認證；HK 掃臉認證只限大陸身份證。
- 做咗嘢記得 update `TODOS.md`（jarvis-pc 已合併主線）＋ `code_change_log.md`。
