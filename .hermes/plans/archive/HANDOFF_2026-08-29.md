# HANDOFF — 2026-08-29（深夜版，含 0.4.9→0.4.10）

> 下次 session 起點：**JARVIS ONE 0.4.10 跑緊**。讀呢份之前先讀 `AGENTS.md`（jarvis-pc 根目錄，2026-08-29 新增——專案 context 自動載入）+ `REMAINING_WORK.md`。
> 新 session 唔好等 SK 叫讀——AGENTS.md 規則已寫明「JARVIS 任務開始前必須讀 plans」。

## 現行狀態（驗證過）

- **JARVIS ONE 0.4.10**：`C:\Users\skps9\Documents\Code_Project\jarvis-hud\dist\JARVIS-ONE-0.4.10.exe`（72MB）；Desktop / Startup / Start Menu 3 個 .lnk 全指 0.4.10
- Sidecar：`python -m jarvis serve`（Electron spawn，JARVIS_ELECTRON_HOST=1 headless）
- Ports：8765（alerts MCP + GET/POST /settings）、8770（reply）、8771（media bridge）；Hermes 8642
- **Hermes Desktop 已裝**（2026-08-29 晚）：`AppData\Local\hermes\hermes-agent\apps\desktop\release\win-unpacked\Hermes.exe`（Electron 40.10.2）；用現有 runtime + venv，起自己 serve（port 隨機），同 8642 gateway 並存；**Quick Entry hotkey 已由 Ctrl+Shift+Space 改做 Ctrl+Shift+Y**（`%APPDATA%\hermes\quick-entry.json`）
- cron：sk-activity-monitor（1m）、Gateway watchdog（2m）——**冇 JARVIS alert cron**（sidecar poller 做緊）

## 今日完成（2026-08-29 深夜 session）

1. **Dock 移除（0.4.9）**：dock.html/dock-preload/probe/apps.json 刪晒；main.js dock 全清（grep 0 命中）；音樂控制搬 tray「音樂控制」submenu；media bridge 8771 + Chrome extension 保留；0.4.8 曾修 #apps→#dockApps（dock 已冇，唔使再理）
2. **H4 secrets DPAPI（0.4.10）**：`src/jarvis/dpapi.py`（ctypes CryptProtectData，prefix `dpapi:`）；settings.json 嘅 llm/asr/mimo_api_key 已加密（真實遷移完成）；load 解密/save 加密；sidecar `GET /settings`（解密版）+ Electron `settings:load` 改經 sidecar；**alerts_mcp_token + hermes_api.key 保持明文**（跨 process bearer，encrypt 會 break 驗證——文檔 `docs/hermes_bridge_auth_rotation.md`）
3. **D1 確認**：Hermes push/notify 已由 sidecar 2s poller 完成（`scripts/hermes_alert_poll_loop.py`，pythonw ~1s）——**唔好加 Hermes cron poll**（會 race/double speak）
4. **F3 vc_fail_closed（0.4.10）**：settings 加 `vc_fail_closed`（default False=fail-open）；activity_monitor `detect_voice_call` pycaw 失敗讀 settings 決定；settings.html toggle + clampSettingsPatch 已加
5. **A3 尾**：renderer/index.html 視覺確認已統一（同一 --blue/字體系），唔 link hud-theme.css 係刻意（全屏 overlay 避免 CSS 衝突）——冇改動
6. **H 節剩低全清**：settings field-map（custom_models 缺口已修）、auth rotation 文檔、tk settings_ui 凍結（settings_ui.py 已加凍結標記）
7. **AGENTS.md 自動載入（本 session 新增）**：`jarvis-pc\AGENTS.md` 新建（專案 context 完整版）+ `C:\Users\skps9\AGENTS.md` 加「JARVIS 專案（自動載入規則）」——下次 session 唔使 SK 叫，自動讀 plans

## 剩低（詳見 REMAINING_WORK.md）

- 🟡 **G 人手實測**（等 SK）：Tier 1（BGM 30s 0 誤觸 + 喊完→有聲 ≤3s）、聲紋 enrollment（Arctis）、AEC voice call 真實場景、Settings tab
- 🟡 **C 擴展連接**（等 credentials）：WhatsApp / Telegram / Hue
- ⏳ 可選：Iron Man 視覺完整化、自訓 wake「jarvis」、Discord 回覆 voice out、D3 HWiNFO SHM/GPU-Z failover
- 💤 deferred：Mage-VL streaming gate（mamba_ssm Windows 唔 practical）

## 陷阱（重溫）

- Settings 單一 writer：改 settings 用 sidecar `POST /settings`（Bearer = `%APPDATA%\Jarvis\alerts\mcp_token.txt`），**唔好直接寫 settings.json**（H4 後 secrets 加密，直接寫會寫壞）
- asar 驗證：`npx --yes @electron/asar list "dist/win-unpacked/resources/app.asar"`；**唔好 extract 入 repo**（會 overwrite source——jarvis-hud 唔係 git repo）；extract 去 /c/Temp
- Python：`C:\Users\skps9\AppData\Local\Python\pythoncore-3.14-64\python.exe`，跑 jarvis 用 `env -u PYTHONPATH PYTHONPATH=...src`
- 換版流程：bump version → `npm run dist` → kill JARVIS（單斜線 taskkill）→ 開新 exe → 更新 3 個 .lnk
- 語音一律英文；GUI 操作前讀 sk_activity.json（playing/using 禁彈窗）
- `dpapi:` 值唔好當明文讀；settings.json 已加密

## 語音/硬體設定（驗證過）

- wake_mic = 「麥克風 (2- Arctis Nova 7)」44.1k；TTS 輸出 = G27Q 螢幕喇叭；AEC reference = Sonar Media + Sonar Chat（唔用 Arctis loopback）
- ⚠️ Arctis 週期性 rms=0.000（headset 休眠/斷連）——叫唔醒先睇 wake_debug.log
- mic 細（avg ~0.05）→ AGC 上線
